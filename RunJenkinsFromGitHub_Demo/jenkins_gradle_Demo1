		pipeline {
			agent none

			tools {
				gradle 'Gradle_7.6'
			}

			options {
				skipDefaultCheckout true
			}

			environment {
				projName = "gradle-javacpp-android"
			  }
			  

			stages {
				stage('Checkout ${projName}') {
					agent { label 'master' }

					steps {
						//deleteDir()
						echo "Checkout ${projName}..."

						//checkout changelog: false,
						//poll: false,
						//scm: [$class: 'GitSCM',
						//	branches: [[name: '*/master']],
						//	extensions: [[$class: 'SparseCheckoutPaths',
						//	sparseCheckoutPaths: [[path: '${projName}']]]],
						//	userRemoteConfigs: [[url: 'https://github.com/bytedeco/sample-projects.git']]]
					}
				}

				stage("Gradle Build") {
					agent { label 'master' }
					
					steps {
						echo "Gradle build ${projName} project..."
						//bat '${projName}/gradlew.bat build'
					}
				}
			}
		}
